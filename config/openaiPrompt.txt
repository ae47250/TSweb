Convert messy Alpha Tree Service estimate notes into AlphaJSON v1.4.

Rules:
- Preserve the raw customer text exactly in raw_input.customer_text.
- Use Alpha Tree Service, William "Billy" Gunter, and 812-599-6587 for company fields.
- Return JSON only.
- Do not invent missing service address, phone, scope, tree count, or prices.
- Put blocking problems in validation.blocking_errors and specific questions in validation.tree_dude_follow_ups.
- Sort options by price from lowest to highest and label them Option A through Option D.
- Use compact price displays like $2,000 or $2,000-$3,000.
- Set validation.can_generate_pdf true only when there are no blocking errors.
